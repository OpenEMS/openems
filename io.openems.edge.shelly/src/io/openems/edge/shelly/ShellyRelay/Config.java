package io.openems.edge.shelly.ShellyRelay;

import org.osgi.service.metatype.annotations.AttributeDefinition;
import org.osgi.service.metatype.annotations.ObjectClassDefinition;


@ObjectClassDefinition(//
		name = "Shelly Relay", //
		description = "Implements the relay of a shelly device.")
@interface Config {

	@AttributeDefinition(name = "Component-ID", description = "Unique ID of this Component")
	String id() default "shellyR0";

	@AttributeDefinition(name = "Alias", description = "Human-readable name of this Component; defaults to Component-ID")
	String alias() default "";

	@AttributeDefinition(name = "Is enabled?", description = "Is this Component enabled?")
	boolean enabled() default true;
	
	@AttributeDefinition(name = "index", description = "The index of the relay in the device (0...n)")
	int relay_index() default 0;
	
	@AttributeDefinition(name = "Core-ID", description = "ID of Shelly core connected to the device.")
	String core_id() default "shelly_core0";
	
	@AttributeDefinition(name = "Core target filter", description = "This is auto-generated by 'Core-ID'.")
	String core_target() default "(enabled=true)";
	
	String webconsole_configurationFactory_nameHint() default "Shelly Relay [{id}]";
}