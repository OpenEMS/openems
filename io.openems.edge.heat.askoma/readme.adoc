= Askoma Heating Element

Smart heating element control via Modbus for thermal load management.

== Overview

This bundle provides integration with Askoma heating elements, intelligent thermal load controllers designed for electric heating applications. The component reads and controls heating elements via Modbus, enabling dynamic thermal load management for peak shaving and demand response scenarios.

Askoma heating elements are suitable for demand response programs, thermal energy storage, and dynamic heating control integrated with renewable energy systems.

== Supported Devices

.Askoma Heating Elements
* **Type**: Smart heating element controller
* **Communication**: Modbus TCP/RTU
* **Typical Use**: Demand response, thermal load management, heating control
* **Control**: On/off or variable power control via Modbus
* **Monitoring**: Power, energy, and status monitoring
* **Documentation**: https://www.askoma.com/de/produkte.html[Askoma Products icon:external-link[]]

== Components

This bundle implements the following OpenEMS Components:

=== <<_askoma_heating,Askoma Heating Element>>

*Name*: Heat Askoma

*Factory-PID*: `Heat.Askoma`

.Implemented Natures/Interfaces
* HeatAskoma
* ModbusComponent
* OpenemsComponent
* ElectricityMeter
* Heat
* ManagedHeatElement
* TimedataProvider
* EventHandler

*Description*: Modbus interface for Askoma heating elements. Monitors heating power consumption and enables dynamic control for demand response and load shifting. Can operate in read-only mode for monitoring only or managed mode for active heating control.

*Configuration*:
.Configuration Parameters
* `id` (String): Component ID for unique identification (default: "heat0")
* `alias` (String): Human-readable alias for the component
* `enabled` (Boolean): Enable/disable this component (default: true)
* `modbus_id` (String): ID of the Modbus bridge to use (default: "modbus0")
* `modbusUnitId` (Integer): Modbus Unit-ID of the heating element (default: 1)
* `readOnly` (Boolean): Read-only mode (monitoring only) vs managed mode (default: true)

.Example Configuration
For an Askoma heating element in managed mode:
* `id`: "heat0"
* `alias`: "Askoma Heating Element"
* `modbus_id`: "modbus0"
* `modbusUnitId`: 1
* `readOnly`: false

[[_askoma_heating]]

== Prerequisites / Dependencies

* Modbus bridge component must be configured and running (e.g., `modbus0`)
* Askoma heating element device must be physically installed and powered on
* Modbus TCP or RTU communication working between system and heating element
* Device must be properly addressed on Modbus bus
* For managed mode: proper electrical installation with safety interlocks
* Firmware on device should be current

== Features

* Real-time power and energy monitoring
* Modbus-based heating element control
* Read-only monitoring or full managed control modes
* Integration with demand response programs
* Thermal load shifting and peak shaving capability
* Time-of-use operation support with timedata provider
* Dynamic control based on grid signals
* Compatible with most energy management controllers
* ElectricityMeter interface for consumption tracking

== Known Limitations

* Requires active Modbus connection - no local operation
* Modbus timeouts will cause temporary data unavailability
* Read-only mode prevents active heating control
* Power output is fixed by heating element design (typically 3-6 kW)
* Heating element must be electrically safe and properly installed
* Cannot modulate power (binary on/off control typical)
* Requires manual safety checks before enabling managed mode

https://github.com/OpenEMS/openems/tree/develop/io.openems.edge.heat.askoma[Source Code icon:github[]]
