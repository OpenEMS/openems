/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {EssData} from './ess-data';

describe('EssData', () => {
  it('should create an instance', () => {
    expect(new EssData()).toBeTruthy();
  });
});
