/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OdooMonitorOneComponent } from './odoo-monitor-one.component';

describe('Component: OdooMonitorOne', () => {
  it('should create an instance', () => {
    let component = new OdooMonitorOneComponent();
    expect(component).toBeTruthy();
  });
});
